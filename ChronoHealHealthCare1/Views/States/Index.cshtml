@model IEnumerable<ChronoHealHealthCare1.Models.State>

@{
    ViewBag.Title = "Index";
}
<div class="container p-5">
    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.States)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.States)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.StateId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.StateId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.StateId })
                </td>
            </tr>
        }

    </table>
</div>