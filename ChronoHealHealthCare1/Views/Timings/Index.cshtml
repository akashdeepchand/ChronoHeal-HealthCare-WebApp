@model IEnumerable<ChronoHealHealthCare1.Models.Timing>

@{
    ViewBag.Title = "Index";
}
<div class="container p-5">
    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Timing1)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Timing1)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.TId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.TId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.TId })
                </td>
            </tr>
        }

    </table>
</div>